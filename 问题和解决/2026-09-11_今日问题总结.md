# 今日问题总结

> 日期：2026 年 9 月 11 日  
> 整理依据：本次对话中的当日问题总结。未看到后续验证记录的问题，保留为待确认状态。

今天遇到的问题主要集中在四个方面：**项目环境搭建、模型原理理解、RAG 与 Agent、面试和简历表达。**

## 一、实际工程问题：BaziMiao 项目没有顺利启动

今天这部分不是一个孤立报错，而是随着安装推进，陆续暴露出来的几个问题。

| 问题 | 今天遇到的具体情况 |
| --- | --- |
| **Python 版本与依赖兼容** | Windows 环境下安装 `sxtwl==2.0.7` 失败，当时排查涉及 Python 3.13 的兼容性和源码构建问题，之后转向 Python 3.11 环境。 |
| **虚拟环境里的 Python 和 pip 没有对应上** | 虽然已经激活 `.venv-wsl`，`python` 指向虚拟环境，但 `pip` 仍指向 `/usr/bin/pip`。执行 `make setup` 时出现 `externally-managed-environment`。 |
| **启动时找不到 Uvicorn** | 依赖安装没有完成时，继续执行 `make dev`，出现 `uvicorn: not found`。 |
| **依赖装好后又遇到端口占用** | 后来重新创建了带 `--seed` 的 Python 3.11 虚拟环境，Python 和 pip 已经指向同一个环境；继续安装、生成 `.env` 并启动后，最后出现 `[Errno 98] Address already in use`。 |

**根据当天总结记录，已经从“环境和依赖装不起来”，推进到“能调用启动程序，但启动端口被占用”。最后这个问题还没有看到解决后的运行记录。**

这部分暴露出的核心疑惑是：**当前命令到底使用哪个 Python、哪个 pip，以及依赖装到了哪里。** 环境被激活，并不代表后续所有命令都一定使用了同一套环境。

## 二、模型原理：主要卡在“公式里的运算到底发生了什么”

### 1. 自注意力：点积、维度和缩放因子没有完全串起来

这是今天追问最深入的一部分。你不是只想记住“除以 √dₖ 是为了稳定”，而是在追问这句话背后的完整逻辑。

| 具体疑问 | 你想弄清楚的内容 |
| --- | --- |
| **为什么均值近似为 0？** | 推导中对 Q、K 的统计假设从哪里来，而不是直接接受这个前提。 |
| **为什么维度越大，点积的数值越容易变大？** | 单项乘积与多项相加之间，方差和尺度有什么关系。 |
| **为什么 Softmax 会变得特别尖？** | 分数差异变大后，为什么输出容易接近 0 或 1，以及这对训练有什么影响。 |
| **为什么除以 √dₖ？** | 为什么是维度的平方根，而不是直接除以维度。 |
| **点积不是只得到一个数字吗？** | 单个 Q 向量和 K 向量的点积，与整个 `QKᵀ` 矩阵计算有什么区别。 |
| **点积之后怎么再乘 V？** | 注意力分数、注意力权重和最终输出的形状如何衔接。 |

**从这些连续追问来看，你主要需要打通的是：一个向量的计算，怎样扩展成整段序列的矩阵计算。** 所以你后来要求一个具体的 `QKᵀ` 手算例子，而不只是继续听抽象解释。

### 2. 均方根归一化（RMSNorm）

你问的是：**这个公式像数学里的什么运算？**

这里的重点是把公式和熟悉的数学概念对应起来，知道它每一步在算什么，而不是只记住“它是一种归一化方法”。

### 3. PPO、DPO、GRPO 与知识蒸馏

强化学习部分，你希望能从**训练方式和流程差别**讲清近端策略优化（PPO）、直接偏好优化（DPO）、组相对策略优化（GRPO），而不是面试时一上来堆公式。

知识蒸馏部分，你重点追问了**硬标签和软标签到底传递了什么不同的信息**，以及怎么把“老师的犹豫程度和相似关系”说得更准确、更自然。你关心的是这句话的实际含义，而不是换几个听起来专业的词。

## 三、RAG、Agent 和后端：主要在厘清不同环节的职责

这里的 RAG 指检索增强生成，Agent 指智能体。

| 主题 | 今天的具体问题 |
| --- | --- |
| **RAG 文本块大小** | 围绕文本块大小的面试回答进行改写，希望表达自然，同时保留必要的原因和逻辑。 |
| **RAG 评测** | 为什么要分检索侧和生成侧评测；召回率、平均倒数排名、归一化折损累计增益等指标分别关注什么；忠实度、答案相关性、上下文相关性的中文含义。 |
| **来源引用与证据支撑** | 你明确提出：“引用来源不一定是 RAG 吧？”同时追问证据支撑（Grounding）的含义，开始区分“答案附有来源”和“答案确实有证据支撑”这些概念。 |
| **相关面试题的覆盖范围** | 围绕用户输入处理方案、来源引用方案整理面试问题；来源引用部分要求只保留概率最高、覆盖面最广的 10 个问题。 |
| **离线 Agent** | 哪些智能体框架能使用本地离线模型，是否有现成的 GitHub 项目，以及有没有一键安装方案。 |
| **FastAPI 分层** | 路由、数据校验、业务逻辑、数据库分别负责什么；尤其是“数据库模型也定义字段，数据校验模型也定义字段，为什么不算重复”。 |

这几部分有一个共同点：**你正在区分“名字相近、看起来都在处理数据”的组件，到底各自解决什么问题。** 比如，数据库表结构与接口数据校验、检索评测与生成评测、来源引用与 RAG，都不能直接当成同一件事。

## 四、面试和简历：内容要准确，也要能自然讲出来

### 1. 项目经历

你今天梳理了 MindTrip 和 AtlasSplit 的时间安排，进一步细化叁昧火游戏工作室的 UE5 经历，也询问了《盲盒生存》《扎格勒》的游戏类型。

你还要求把工作室经历拆成**“主要工作”和“成果”**两部分，再压缩表述。

### 2. 回答措辞

你反复调整自注意力、强化学习、蒸馏和 RAG 的回答。你的要求已经比较明确：**像真人说话，但不能为了简短，把关键前提、因果关系和必要细节删掉；专业术语也要有中文解释。**

## 五、今天最核心的三个卡点

### 1. 工程上：运行环境的一致性

你需要分清 Python、pip、虚拟环境、依赖和启动进程之间的关系。当前尚未确认解决的是 BaziMiao 的端口占用。

### 2. 原理上：从公式到具体计算的衔接

最典型的是自注意力。你已经不满足于背结论，而是要知道每一步输入什么、输出什么、维度为什么这样变化。

### 3. 表达上：把“知道术语”变成“能完整解释”

你今天不断要求重写，其实是在校正两种问题：一种是表述太抽象，另一种是为了口语化而省掉了关键逻辑。
