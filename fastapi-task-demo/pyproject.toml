# uv 会读取这个文件来管理项目元数据和依赖。

[project]
name = "task-api-split"
version = "0.2.0"
description = "FastAPI + PostgreSQL + SQLAlchemy 异步学习项目"
requires-python = ">=3.10"
dependencies = [
    "alembic>=1.16.0",
    "asyncpg>=0.30.0",
    "fastapi[standard]>=0.141.1",
    "pydantic-settings>=2.10.0",
    "sqlalchemy>=2.0.40",
]
