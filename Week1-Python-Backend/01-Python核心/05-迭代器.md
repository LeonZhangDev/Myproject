---
tags: [week1]
difficulty: P0
status: learning
---

# 迭代器

## 一句话理解

迭代器实现迭代协议，每次只给一个元素。

## 核心理解

`for` 循环内部会不断调用 `next()`，直到 `StopIteration`。

## 最小代码 / 场景

```python
it=iter([10,20]); print(next(it)); print(next(it))
```

## 🔗 关联知识

- [[06-生成器与yield]]
