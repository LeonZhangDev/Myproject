---
tags: [week1]
difficulty: P0
status: learning
---

# FastAPI 异常处理

## 一句话理解

把业务异常集中映射成稳定 HTTP 错误响应。

## 核心理解

不要每个路由重复 try/except；统一 exception_handler 更易维护。

## 🧠 完整理解

### 我会先这样理解

FastAPI 异常处理分成业务主动抛出的 HTTPException、请求校验异常以及未处理的系统异常。更大的项目通常定义领域异常，再用全局 exception handler 统一映射到稳定的错误码和 JSON 结构。这样 endpoint 不需要到处复制 try/except，日志与对外错误也能分层。

### 执行顺序 / 思考顺序

业务层抛领域异常/endpoint 抛 HTTPException → 异常沿调用栈向上 → FastAPI/Starlette 查找最匹配 handler → handler 记录必要上下文并构造 Response → 返回客户端。未匹配异常通常进入 500 处理。

### 容易踩的坑

不要把数据库异常文本、traceback 原样返回前端；也不要把所有异常统一改成 200 + `{"code":500}`，否则会破坏 HTTP 语义和监控。

### 面试时我会这样说

> 我通常让业务层表达业务异常，API 层统一转 HTTP。比如 `TaskNotFound` 最后变 404；真正未知异常记录 request-id 和堆栈后返回统一 500。这样既不泄露内部细节，也不用每个路由重复 try/except。

## 最小代码 / 场景

```python
@app.exception_handler(AppException)
async def handler(request, exc):
    return JSONResponse(status_code=exc.status_code, content={'code':exc.code,'message':exc.message})
```


## 🧪 简单例子与返回结果

### 例子

```python
from fastapi import FastAPI, HTTPException
app = FastAPI()

@app.get("/users/{user_id}")
def get_user(user_id: int):
    if user_id != 1:
        raise HTTPException(status_code=404, detail="user not found")
    return {"id": 1}
```

### 运行 / 返回结果

```text
GET /users/99
404 Not Found
{"detail":"user not found"}
```

## 🔗 关联知识

- [[09-JSONResponse]]
- [[../01-Python核心/11-异常处理]]
