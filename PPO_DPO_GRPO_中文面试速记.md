# PPO、DPO、GRPO 对比速记

## 一、核心对比

| 方法 | 英文全称 | 中文名称 | 一句话理解 | 主要模型及作用 |
|---|---|---|---|---|
| **PPO** | Proximal Policy Optimization | **近端策略优化** | 根据奖励和优势值更新模型，同时限制每次更新不要太大。 | **策略模型（Actor / Policy）**：负责生成回答，并被训练更新；**奖励模型（Reward Model）**：给回答打分；**价值模型（Critic / Value Model）**：估计当前状态价值，辅助计算优势值；**参考模型（Reference Model）**：作为原模型基准，限制策略模型不要偏离太远。 |
| **DPO** | Direct Preference Optimization | **直接偏好优化** | 直接使用“更好的回答”和“更差的回答”进行偏好训练，不需要完整的强化学习流程。 | **策略模型（Policy Model）**：提高优选回答的概率，降低拒绝回答的概率；**参考模型（Reference Model）**：提供原始模型概率作为约束。通常**不需要奖励模型和价值模型**。 |
| **GRPO** | Group Relative Policy Optimization | **组相对策略优化** | 同一个问题生成多条回答，根据这一组回答之间的相对奖励计算优势值，再更新模型。 | **策略模型（Policy Model）**：对同一个问题生成多条回答并进行更新；**奖励模型或奖励函数**：分别给这些回答打分；**参考模型（Reference Model）**：通常用于限制模型偏离原模型；**不需要价值模型（Critic）**，因为优势值可以直接通过组内奖励比较得到。 |

---

## 二、PPO

### 近端策略优化（PPO）

核心思想：

> **让高奖励回答的概率变高，让低奖励回答的概率变低，但每次不要改得太猛。**

主要模型：

1. **策略模型**
   - 负责生成回答。
   - PPO 最终主要更新的就是它。

2. **奖励模型**
   - 判断回答质量。
   - 给生成结果一个奖励分数。

3. **价值模型**
   - 预测当前状态未来大概能获得多少奖励。
   - 用来辅助计算优势值。

4. **参考模型**
   - 一般是训练前的原始模型。
   - 用来计算 KL 散度，防止新模型偏离原模型太远。

### 记忆

**PPO = 奖励模型 + 价值模型 + 策略更新 + 更新幅度限制**

---

## 三、DPO

### 直接偏好优化（DPO）

核心思想：

> **直接告诉模型：回答 A 比回答 B 好，让模型提高 A 的概率、降低 B 的概率。**

训练数据一般长这样：

```text
问题
↓
优选回答（Preferred）
拒绝回答（Rejected）
```

主要模型：

1. **策略模型**
   - 学习提高优选回答的生成概率。
   - 降低拒绝回答的生成概率。

2. **参考模型**
   - 提供原始模型的概率基准。
   - 防止训练后的模型偏离太远。

DPO 通常不需要：

- 奖励模型
- 价值模型
- PPO 的在线强化学习训练过程

### 记忆

**DPO = 直接学“哪个好、哪个不好”**

---

## 四、GRPO

### 组相对策略优化（GRPO）

核心思想：

> **同一个问题生成一组回答，不单独训练价值模型，而是直接比较这一组回答谁更好。**

例如：

```text
同一个问题
↓
回答 A：奖励 0.9
回答 B：奖励 0.7
回答 C：奖励 0.3
回答 D：奖励 0.1
```

然后根据这一组回答的平均水平，判断：

```text
A：明显高于平均水平
B：略高于平均水平
C：低于平均水平
D：明显低于平均水平
```

再根据这种**相对优势**更新策略模型。

主要模型：

1. **策略模型**
   - 针对同一个问题生成多条回答。
   - 根据组内比较结果进行训练更新。

2. **奖励模型或奖励函数**
   - 给每一条回答打分。
   - 可以是训练出的奖励模型，也可以是规则奖励。

3. **参考模型**
   - 通常用来做 KL 约束。
   - 防止策略模型偏离原模型太远。

GRPO 的重要特点：

> **不需要单独训练价值模型（Critic）。**

### 记忆

**GRPO = 同题多答 + 组内比较 + 不需要 Critic**

---

## 五、面试一句话回答

### PPO

**PPO，也就是近端策略优化，是通过奖励模型和价值模型计算优势值来更新策略模型，同时利用裁剪和 KL 约束限制每次更新幅度。**

### DPO

**DPO，也就是直接偏好优化，是直接使用优选回答和拒绝回答进行偏好训练，提高好回答的概率、降低差回答的概率，因此不需要单独训练奖励模型和价值模型。**

### GRPO

**GRPO，也就是组相对策略优化，是让模型针对同一个问题生成多条回答，通过组内奖励比较计算相对优势，再更新策略模型，因此可以省掉价值模型。**

---

## 六、最短记忆口诀

```text
PPO：奖励 + Critic + 限制更新幅度

DPO：好回答 > 差回答，直接学偏好

GRPO：同题多答 + 组内比较，不需要 Critic
```

## 七、模型数量快速记忆

| 方法 | 常见组成 |
|---|---|
| **PPO** | 策略模型 + 奖励模型 + 价值模型 + 参考模型 |
| **DPO** | 策略模型 + 参考模型 |
| **GRPO** | 策略模型 + 奖励模型/奖励函数 + 参考模型 |

> 注意：这里说的是经典或常见实现，具体工程实现中模型可能共享参数，奖励也可能直接使用规则函数，因此实际部署时不一定真的运行这么多份完全独立的大模型。
