# 检索增强生成用户输入处理：10 个核心面试问题与完整参考答案

> **整理依据：**《RAG 用户输入侧方案与具体实现技术报告（详细工程版）》。
>
> **内容范围：**沿用上一轮的 10 个问题，讨论会话上下文、查询理解、查询改写、检索策略、混合检索、重排序和失败恢复，不是来源引用专项问答。
>
> **使用边界：**下面的“我会”表示对这份方案的设计与实现思路，不表示所有模块已经在项目中落地。轮数、查询数量、候选数量、温度参数和重试次数，均按原报告中的建议起点讲述，不代表已经测得的最佳配置。原文没有展开的实现细节，会单独说明。

---

## 1. 用户输入进入检索增强生成系统后，到返回检索证据之前，完整处理流程是什么？

**面试问题：**LangGraph、LlamaIndex、BGE-M3 和重排序模型分别负责什么？为什么不直接把用户输入丢进向量检索？

### 参考答案

我会先把这条链路分成两件事：第一件事是弄清楚用户到底要查什么，第二件事才是把能回答这个问题的资料找回来。不能直接把这两件事合并成一次向量检索，因为用户的输入不一定是一个完整、清楚、适合检索的问题。

比如，用户上一轮说想去四姑娘山，这一轮只说“改成三天，两个人，预算三千，不自驾”。单看这一句话，目的地就缺失了。如果直接检索，系统可能找回来很多三日游攻略，却不一定是用户要去的地方。所以，我会先按当前会话读取最近的聊天记录；如果已经有会话摘要，也一起加载，帮助系统恢复省略的地点和已经确认的限制。

接下来，我会做查询理解，把用户的需求整理成结构化信息。这里既要知道用户是在问一个事实、寻求推荐，还是要求做完整规划，也要提取目的地、出发地、天数、人数、预算、交通限制和偏好。规则能稳定提取的字段先用规则，语义比较模糊的部分再交给大语言模型。

在这些信息的基础上，系统决定用哪种检索策略。问题已经完整、目标也单一，就直接检索；问题依赖上一轮，就先补全上下文；同一个目标比较宽泛，可以生成几个不同检索角度；包含交通比较、景点安排和预算验证等多个任务，就拆成不同子问题。这里不是让每个问题都依次做改写、扩展、分解，而是根据问题选择有必要的处理。

真正执行检索时，我会把查询和结构化过滤条件交给独立的 LlamaIndex 检索服务。它先根据城市等条件限制检索范围，再执行稠密检索和稀疏检索，把多路结果融合、去重，最后用重排序模型重新判断哪些候选内容更贴近用户的问题。

拿到排序后的结果，还不能直接认为检索成功。系统需要检查有没有结果、目的地是否匹配、是否保留来源标识，以及复杂问题的各个必要子任务有没有对应证据。通过检查的资料才进入后续规划和生成；没有通过，就根据失败原因进入纠正性改写，必要时再使用假设文档辅助检索。

这几个组件的职责，我会明确分开。**LangGraph 负责决定为什么查、查什么、走哪条路线，以及失败后怎么办；LlamaIndex 负责组织索引、过滤、召回、融合和结果返回。BGE-M3 在这份方案里负责生成稠密或稀疏表示，帮助召回候选资料；重排序模型则对查询与候选文档进行更精细的相关性判断。**

在服务边界上，原报告建议检索服务接收查询列表、过滤条件和各阶段候选数量，返回来源标识、片段标识、标题、正文、来源地址、相关性分数和元数据。这样主流程不仅能看到“找到了什么”，也能知道结果来自哪里、在哪一步被选出来，方便后续排查问题。

因此，我不会把整个系统描述成“把一句话改写一下，再查向量库”。更准确的说法是：先恢复需求和约束，再选择合适的检索策略，最后检查证据是否真的够用。用户输入理解和知识检索各有自己的职责，问题出在哪里也更容易定位。

**原文依据：**第 1 节、第 14 节、第 16 节、第 20 节。

---

## 2. 多轮对话中，用户说“刚才那个地方”“改成三天”时，系统如何理解他的真实需求？

**面试问题：**最近对话、滑动窗口、词元预算和会话摘要应该怎样配合，才能避免丢失重要约束？

### 参考答案

我会先把它当成一个会话上下文管理问题，而不是一上来就做长期记忆检索。因为“那个地方”“第二个方案”这类说法，通常指向最近几轮对话，先把当前会话的近期内容读出来，往往就能找到对应的信息。

这套设计可以利用已有的会话表和聊天内容表，每条消息带有会话标识、角色、内容和创建时间。所以第一步可以直接根据当前会话标识查询最近的消息。数据库里按时间倒序取，取出来后在应用层反转成时间正序，再交给后续的上下文理解模块。

第一版我会从最近四到五轮对话开始，也就是正常一问一答情况下大约八到十条消息。这是一个起点，不是说系统永远只能记住五轮。窗口的作用，是保留近期最可能相关的信息，同时避免把很久以前已经结束的话题全部带进来。

但是，只限制消息条数还不够。用户的一条消息可能只有几个字，助手的一条回复也可能是一整份长行程。同样是十条消息，实际占用的上下文长度可能差很多。所以还要限制词元数量，也就是按照模型实际处理的文本单位控制长度，而不是只数消息。

上下文预算可以先从一千五百到两千五百个词元尝试。具体处理方法是从最新消息往前累计，达到预算后停止加入更旧的消息，最后再把保留的内容恢复成时间正序。这样能优先保住近期信息，避免历史越聊越长，导致每次改写的输入也不断膨胀。至于最终能分配多少预算，还需要结合实际模型的上下文容量确定，不能把这个起点直接当成所有模型都适用的配置。

滑动窗口也有一个明显问题：一些重要约束可能在很早以前就说过，比如预算五千、不自驾、带老人，或者酒店已经订好了。如果这些消息滑出窗口，不能就当作用户没说过。因此，对于长对话，我会再配一个会话摘要，专门保留影响后续决策的关键信息。

这个摘要不是普通的聊天内容概括。我会要求它保留地点、日期、人数、预算、交通和住宿限制、用户偏好、拒绝过的方案，以及已经确认的决定。否则，摘要虽然读起来流畅，却可能把“不要徒步”这类真正影响检索的条件省掉。

触发摘要更新的时机，可以先考虑消息超过二十条，或者历史词元超过四千的情况。这些同样只是示例阈值。更新时使用旧摘要加上新进入摘要范围的较早消息，生成新的摘要，而不是每次都把整段历史重新处理一遍。最终交给查询改写模块的是三部分：会话摘要、近期消息和当前问题。

所以我的理解是，**近期消息负责保留对话细节，滑动窗口负责限制历史范围，词元预算负责控制输入长度，会话摘要负责保住滑出窗口的重要约束。**它们解决的不是同一个问题，而是互相配合。

至于把全部旧对话转成向量，再按当前问题检索历史消息，原报告把它放在后续能力里。对于第一版，我不会因为要解决“刚才那个地方”就额外引入一套向量记忆系统。实施时可以先完成近期历史读取，对话变长后再补充摘要；等到仅靠这两部分确实找不回相关旧信息时，再考虑语义历史检索。

**原文依据：**第 2.1—2.9 节、第 19 节。

---

## 3. 查询改写具体怎么实现，什么时候需要改写，什么时候可以直接检索？

**面试问题：**如何生成不依赖上下文的独立查询，同时避免丢失否定条件、修改预算或引入用户没说过的信息？

### 参考答案

我会先判断当前问题能不能独立理解，而不是默认每次都调用一个模型改写。比如“九寨沟在哪里”已经有明确对象和明确问题，直接检索就可以了。再让模型换一种说法，不一定带来收益，反而增加一次调用和改变原意的机会。

真正需要改写的是有指代、省略，或者表达不完整的情况。比如用户前面说想去四姑娘山，并明确不自驾，这一轮问“那里两天够吗”。这时候改写的目标不是把句子写得更漂亮，而是把它补成“不自驾情况下，四姑娘山两日旅行是否可行”。改写之后，即使检索服务看不到聊天记录，也能明白要查什么。

具体实现上，我会使用大语言模型，配合提示词、结构化输出和 Pydantic 数据校验。模型的输入有三部分：会话摘要、最近聊天记录和当前问题。提示词要明确告诉模型，它是检索查询改写器，不是问答助手，任务是恢复用户的完整信息需求，而不是直接给旅行建议。

我会把必须保留的内容写清楚，包括地点、日期、天数、人数、预算、交通方式、否定条件和已经确认的偏好。同时明确禁止回答问题、补充景点、编造价格，或者自行加入交通方案。这样做是为了限制改写的范围：可以补回上下文里已经存在的信息，不能替用户创造新的需求。

输出也不应该只有一句改写结果。结构里还要保留原始问题、改写后的独立查询、是否依赖上下文、是否发生改写，以及保留下来的实体和约束。比如改写结果里应能看到“四姑娘山”这个实体，也能看到“两天”和“不自驾”这两个条件。这样出了问题以后，才有条件对照原始输入检查，而不是只剩下一句无法追踪来源的新问题。

这里我会特别区分两种校验。Pydantic 可以检查输出结构和字段类型是否符合要求，但不能因为格式正确，就认为用户的意思一定保留完整了。模型完全可能输出一份结构正确的数据，却漏掉“不自驾”。所以结构化输出解决的是结果能不能被程序稳定接收，不等于已经解决了语义是否准确的问题。

对于语义，我最关心的是关键实体和约束有没有改变。例如三天不能变成五天，两个人的预算不能被写成人均预算，“不徒步”不能被改成“适合徒步”。提示词和保留字段能够帮助约束这些风险，但不能替代对实际改写结果的验证。因此，我会把这些条件作为重点检查对象，而不会说用了数据校验库就能保证改写不出错。

参数上，我会先使用较低的生成温度，例如零到零点二，因为这里追求的是稳定和忠实，不是创造性。多生成几个有趣的表达对这个环节没有太大价值，准确保住用户的意思才重要。

所以我会把查询改写概括成一句话：**把依赖上下文的问题补完整，但不替用户增加决定。**能直接检索就不改写；确实需要改写时，保留原始输入、明确约束、使用结构化结果，并把语义是否发生变化作为单独的检查重点。

### 原文边界

原报告给出了提示词要求、结构化字段和数据校验方向，但没有提供完整的自动语义一致性校验实现。答案中的关键约束检查属于需要验证的要求，不代表这套自动检查已经完成。

**原文依据：**第 3.1—3.6 节。

---

## 4. 如何把用户的自然语言需求转换成结构化查询条件？

**面试问题：**哪些信息适合用规则提取，哪些需要大语言模型，实体名称如何统一，哪些条件应该用于元数据过滤而不是语义检索？

### 参考答案

我会先区分查询改写和查询理解。查询改写解决的是“这句话完整地在问什么”，查询理解解决的是“这个需求里面有哪些可以交给程序使用的业务信息”。前者的主要结果是一条完整查询，后者的结果更像一份结构化的查询计划。

比如用户说：“成都出发去四姑娘山三天，两个人，预算两千五，不自驾，想轻松一点。”改写可以把这句话整理完整，但如果后面要选择检索策略、过滤目的地、检查预算，就不能一直把这些信息藏在一句自然语言里。我会把出发地、目的地、天数、人数、预算、交通限制和行程偏好分别提取出来。

这里我不会全部依赖大语言模型。像“三天”“两个人”“预算两千五”这种表达，我会优先使用正则表达式和规则解析。因为它们相对明确，很多情况下不需要让模型再理解一次。而“想轻松一点”“喜欢有历史感的地方”“最好不要一直换酒店”，更多涉及语义，适合交给模型做结构化提取。

提取后的查询计划，除了这些业务字段，还要记录用户意图、问题复杂度、准备采用的检索策略、语义查询内容、过滤条件，以及是否需要检索。这样后面的路由和检索节点可以直接读取字段，不用每走一步都重新解释用户说了什么。

实体名称也要统一。比如“成都”“成都市”“四川成都”，表达可能不同，但在这个例子里指的是同一个城市。如果直接拿原始文字做过滤，知识库里存的是“成都”，用户写的是“成都市”，就可能出现不必要的匹配问题。因此，我会使用别名映射，把这些表达归一到统一名称；更正式的做法，是使用城市、目的地或景点的统一标识。

之后，我会把信息分成两类。城市、目的地等明确且能够对应知识库字段的条件，优先变成元数据过滤，用来限制在哪个范围里找资料。“低强度”“历史文化”“适合年轻人”这种偏向内容含义的描述，则继续放在语义查询里，让检索模型判断哪些文本更符合这种需求。

比如要找成都的低强度历史文化景点，可以把“城市是成都”放进过滤条件，把“低强度历史文化景点”作为语义查询。这里两部分的作用很清楚：过滤条件限制范围，语义查询寻找范围内更符合需求的内容，而不是让一个向量相似度分数同时承担所有判断。

当然，能不能过滤，还取决于知识库有没有对应的元数据。原文给出了城市等过滤示例，但没有完整列出所有字段的实际覆盖情况，所以不能把“计划支持某个条件”直接说成“现有数据一定可以精确过滤这个条件”。

我也会注意区分用户明确说了什么，和系统根据业务习惯推断了什么。原文示例把“不自驾”映射成了“公共交通”，但原文没有给出排除其他非自驾方式的依据。因此，这个例子可以说明结构化结果的形态，却不能被当成通用的等价转换规则。用户明确表达的否定条件必须保留，不能在提取过程中悄悄收窄成另一个未经确认的选择。

所以这一步的重点不是尽量多提字段，而是**把可以确定的信息稳定提取出来，把需要语义理解的信息交给模型，再把实体名称和检索条件整理成程序能够可靠使用的结构。**字段看起来完整，不代表每一项都可以由系统自行补齐。

**原文依据：**第 4.1—4.5 节、第 8 节。关于“不自驾”与“公共交通”的区别，是对第 4.4 节输入和输出之间推断边界的说明，并非原文已经实现的校验规则。

---

## 5. 如何设计查询路由，决定使用直接检索、上下文改写、多查询扩展还是查询分解？

**面试问题：**规则和大语言模型如何分工，才能兼顾策略准确性、调用成本与响应延迟？

### 参考答案

我会先把可以选择的路线收敛到少数几种，再用规则优先、大语言模型兜底的方式做判断。这样系统的行为比较明确，也不会为了判断一个简单问题该怎么查，先额外调用一次模型。

这份方案主要有四种路线。第一种是直接检索，适合对象明确、只有一个问题、没有上下文指代，也没有复杂比较的输入。比如“九寨沟海拔多少”，它已经是一条完整的检索问题，没有必要先扩展成好几个查询。

第二种是上下文改写，适合“那里适合老人吗”“第二个方案贵不贵”这类依赖前文的表达。这里首先缺少的不是检索数量，而是查询本身没有说完整，所以应该先恢复指代，再把完整问题交给检索服务。

第三种是多查询扩展，适合目标相同但表达比较宽泛的推荐问题。比如“成都有什么适合年轻人的地方”，只查这一种说法可能覆盖不够，可以围绕已经确认的偏好，从热门景点、拍照休闲或美食街区等不同角度组织查询。但这些查询仍然服务于同一个推荐目标。

第四种是查询分解，适合多个目标、多个约束、复杂比较或完整规划。比如用户要求比较两种交通方式，再做一个三日行程，还要验证预算，这时需要解决的是几个不同任务，不只是把同一句话换几种表达。

规则主要负责识别这些比较确定的情况。原报告的示例先检查是否存在多个目标，再检查上下文依赖，接着判断是不是宽泛推荐，否则直接检索。规则不能确定时，才让模型根据查询计划进行分类。这样不是完全不用模型，而是避免把规则已经能判断的事情再交给模型重复做一次。

模型输出也要受限制。我会让它返回检索策略和选择原因，程序只读取明确的策略字段，原因用于日志和调试。不能让程序去猜模型解释里哪句话才是最终决定，更不能让模型随意生成一个系统并不支持的流程名称。

从成本和延迟上看，我关注的是每一次模型调用有没有实际作用。读取历史、按规则识别明确字段、执行确定的路由判断，本来就不一定需要生成模型。只有上下文补全、语义提取、扩展查询、拆分复杂任务，或者规则确实无法分类时，才需要相应的模型能力。

在框架分工上，我会让 LangGraph 保存选择的策略、生成的查询和后续执行状态，让 LlamaIndex 接收明确的检索任务。这样路由决定是可见的，后续分析“为什么这个问题被拆开”“为什么这次查了三次”也有依据，而不是把策略藏在检索服务内部。

我的核心思路是：**简单问题走简单路线，信息不完整就先补完整，表达覆盖不足就扩展，同一个请求里有多个任务就分解。**不是使用的技术越多越好，而是每个处理步骤都要解决当前问题真实存在的不足。

### 原文边界

这里有一个需要如实说明的地方：原文总架构把上下文改写画成路由分支，但最终案例又展示了先补全“刚才那个地方”，再选择查询分解的过程。两处表达没有完全收敛成唯一的执行顺序。因此，我可以说明上下文补全和策略选择分别解决什么问题，但不能声称报告已经把所有组合情况的节点顺序都定义好了。

**原文依据：**第 5.1—5.5 节、第 6.7 节、第 15 节、第 20 节、第 21 节。

---

## 6. 多查询扩展和普通同义词替换有什么区别，具体怎样生成有价值的扩展查询？

**面试问题：**如何控制查询数量和多样性，并在最终重排序时避免偏离用户的原始意图？

### 参考答案

我理解的多查询扩展，不是简单把几个词换成近义词，而是在同一个用户目标下，从不同的检索角度组织问题。它要解决的是“知识库中的相关资料不一定使用和用户一样的表达”，而不是把一个需求变成几个新的需求。

比如，用户要找成都三日游适合年轻人的地点，而且已经表达了拍照、美食和休闲偏好。系统可以分别查询年轻人热门景点、适合拍照和休闲的地点、适合美食体验的街区。这几个查询的侧重点不同，但都服务于同一个旅行推荐目标，并没有额外替用户增加新的硬性要求。

如果只是把“年轻人”换成“青年”，其他内容完全不变，可能仍然只会找到很相近的一批资料。多查询扩展更关注资料覆盖角度，而不是句子的表面差异。不过，不同查询是否真的带来额外有效结果，仍然需要检索评测判断，不能只因为生成了三句话就认定召回已经改善。

具体实现上，我会使用大语言模型生成查询，并要求结构化输出。输入是已经补全上下文的独立查询，以及结构化的查询计划。这样模型既知道完整问题，也能看到目的地、偏好和限制，不需要根据一句模糊输入自由发挥。

我会要求每个扩展查询都保留核心实体，服务于同一个目标，不回答问题，不生成事实，也不新增用户没有提出的硬约束。输出除了查询文字，还要包含这个查询的检索侧重点。这样可以检查几个查询是不是其实都在找同一种资料，也方便后续分析哪些角度有用、哪些只是重复。

数量上，我会先生成两到三个，并把上限设为三个。这里不是越多越好，因为每增加一个查询，都会带来额外的召回结果和处理成本，也可能增加无关内容。生成温度可以比上下文改写稍高，先尝试零点二到零点四，让表达有一定差异，但仍然以不偏离原意为前提。

我会把生成过程放在 LangGraph 中，让查询列表显式进入状态，再交给 LlamaIndex 执行检索。这样主流程知道具体生成了哪些查询，也能把它们和原始需求、最终证据对应起来。我不倾向于把这些决策全部隐藏在检索服务内部，因为后续追踪、调试和评测会更困难。

检索完成后，多路结果要先融合、去重，再用用户原本的完整问题做最终重排序。这里的“原本的完整问题”是已经解析指代、补齐上下文的独立查询，不是只写着“那里怎么样”的原始短句。

这样做的原因是，扩展查询可能把检索范围带宽。比如拍照方向找到了很多热门街区，但其中一部分并不符合用户已经确认的休闲要求。如果最后只围绕“拍照”这个扩展查询排序，它可能排得很高；再对照完整需求排序，才有机会把结果重新拉回用户真正要解决的问题。

所以这条路线可以概括为：**用几个不同角度扩大有效召回，再用同一个完整需求收回排序方向。**扩展负责找到更多可能相关的资料，最终重排序负责重新对齐原始意图，两步不能混为一谈。

**原文依据：**第 6.1—6.7 节、第 10.1 节。

---

## 7. 查询分解和多查询扩展有什么区别，复杂问题应该如何拆成可检索的子问题？

**面试问题：**多个子问题如何并行检索，并保证交通、住宿、预算等不同需求都有对应证据，而不是被全局排名挤掉？

### 参考答案

我会先看拆出来的查询是不是在解决不同任务。多查询扩展是同一个问题的不同检索角度，查询分解则是把一个复杂请求拆成几个需要分别解决的子任务。这一点非常关键，因为它不仅影响怎么生成查询，也影响最后应该怎样组织证据。

比如“给我规划一次从成都出发、两个人、三天、预算两千五、不自驾的四姑娘山旅行”，里面至少涉及交通是否可行、景点怎样安排，以及预算能不能覆盖主要费用。交通资料、景点资料和费用资料的作用不同，不能指望找到几篇相关攻略，就自动说明这几个问题都已经有了可靠依据。

查询分解可以交给大语言模型完成，但输入不是只有原始问题，还要包括补全后的独立查询、结构化约束和用户意图。这样拆分时就能围绕已经明确的需求展开，而不是让模型随意添加一长串旅行任务。

输出采用结构化形式，每个子问题包括标识、查询内容、任务目的和优先级。比如交通子问题用于确定出行方式、时间和费用；景点子问题用于安排三天的活动；预算子问题用于检查两个人在既定天数和交通限制下的主要花费。任务目的要写清楚，因为后面判断证据是否够用时，需要知道每次检索到底想支撑什么决策。

数量上，我会让第一版最多拆出三个子问题，后续有需要再增加到四个，而不是默认拆出五到十个。拆得太细会增加检索和组织结果的负担，也容易出现很多高度重叠的任务。住宿等内容是否需要单独拆出，要结合用户明确需求和有限的任务数量处理；原文并没有规定每次旅行规划都必须使用一套固定的子问题模板。

对于可以独立查询的子任务，我会使用 Python 的异步任务聚合功能，例如 `asyncio.gather()`，并行发起检索。这样交通、景点和费用资料可以同时找，不必等前一个检索完成才开始下一个。

但我不会把“支持并行检索”讲成“所有多跳问题都可以同时执行”。如果后一个子问题必须使用前一个子问题查到的信息，执行前就还缺输入。原报告虽然把多跳问题列为查询分解的适用场景，但没有展开这种有依赖任务的调度，所以不能据此声称完整的多跳调度已经实现。

结果返回后，最重要的是按子任务保留证据，而不是把所有资料混在一起，只取一个全局前五名。因为某个主题的资料可能特别多，例如交通相关文档占了前几名，预算证据就被挤掉了。最后交给规划模型的资料虽然看起来都相关，却不能支撑所有必要决策。

我会让每个子问题先保留排名最靠前的两到三条证据，再按照交通、景点、预算等任务类别交给后续规划模块。质量检查也要看必要子任务是否都有证据，不能只看总共返回了多少条资料。

所以，**多查询扩展追求的是同一个目标下的检索覆盖，查询分解追求的是复杂需求中的任务覆盖。**前者可以围绕完整问题做最终全局排序；后者还必须保护各个必要子任务的证据，避免一个热门主题占满全部上下文。

**原文依据：**第 6.3 节、第 7.1—7.5 节、第 10.2 节、第 20 节。原文未展开存在依赖关系的多跳子任务调度。

---

## 8. 稠密检索和稀疏检索为什么要结合使用？

**面试问题：**多路检索结果如何通过倒数排名融合、去重和重排序得到最终证据，排名融合与重排序分别解决什么问题？

### 参考答案

我会把两种检索看成互相补充。稠密检索更适合根据内容含义寻找相关资料，稀疏检索更强调具体词项和关键词匹配。用户的问题既可能需要理解语义，也可能包含不能弄错的地名、站名或景点名称，所以只依赖其中一种方式，容易遗漏另一类信息。

在这套设计里，稠密检索使用 BGE-M3 的稠密向量表示。比如用户说“不想太累”，知识库里不一定也写这几个字，而可能写“游览强度较低”或“适合老人”。这里需要找到含义相关的表达，不能只看文字是不是完全一样。

稀疏检索在这份方案里可以使用 BGE-M3 的稀疏表示，主要帮助匹配精确实体、专有名词和关键词，例如“四姑娘山”“双桥沟”“成都东站”。原报告还列出了传统关键词检索作为替代方向，但没有展开其算法计算过程。这道题里需要讲清楚的是，两条召回路线关注的匹配信号不同，因此有组合使用的价值。

具体流程是：先根据目的地等结构化条件限定范围，再分别进行稠密召回和稀疏召回。如果前面还生成了多个扩展查询，就会进一步得到多个查询对应的结果列表。这时需要把它们整理成统一的候选集合。

这里可以使用倒数排名融合。它的作用是结合文档在不同结果列表中的排名，而不是只看某一路原始分数的数值大小。因为向量相关性、关键词相关性和后续重排序分数来自不同处理环节，不能只因为某个分数更大，就直接认为它表示更强的相关性。

融合之后还要去重。相同资料可能被不同查询或不同召回方式重复找到，不能因此在最终上下文里重复占据多个位置。原报告的返回结构包含来源标识和片段标识，能够为区分来源与片段提供信息，不过原文没有给出最终使用的去重键和完整合并代码，因此我不会把某一种去重实现说成已经确定的项目事实。

然后才是重排序。我会使用方案中选定的 `BAAI/bge-reranker-v2-m3`，把查询和候选文档作为一对输入，交给交叉编码器计算相关性分数。这里不是简单地再次合并排名，而是重新看查询与文档内容是否匹配。

因此，融合和重排序解决的问题不同。**融合是在多路召回之间整合候选，重排序是在候选集合里重新判断谁更值得保留。**融合使用了多路检索的信息，但不等于已经完成了最终的内容相关性判断；重排序能够进一步筛选，也不能把本来没有召回的文档凭空找回来。

数量方面，初始配置可以是每个查询在两条召回路线中各取前十到十五条，融合后以二十到三十条候选作为进入重排序的参考规模。在多查询扩展的示例里，最后保留五到八条。实际数量还会受到可用结果和去重情况影响，不能把这些建议值说成每次一定能得到的文档数量，更不能未经评测就称为最佳参数。

最后我会根据上游策略组织证据。如果是多查询扩展，用补齐上下文后的原始需求做最终排序，避免被某个扩展角度带偏；如果是任务分解，则要分别保留必要子任务的证据，不能只用一个全局排名决定所有内容。

### 原文边界

原文没有展开倒数排名融合的计算公式，也没有给出已验证的融合参数。因此，这份方案能支持的是上述组合方式、职责划分和候选数量起点，不能从中推导出某组公式参数已经在项目中完成调优。

**原文依据：**第 8 节、第 9.1—9.4 节、第 10 节、第 16 节。

---

## 9. 如何判断一次检索得到的证据是否足够支撑后续回答？

**面试问题：**检索质量检查应考虑哪些信号，为什么不能直接套用固定的重排序分数阈值，应该如何利用自己的评测集确定阈值？

### 参考答案

我不会把“返回了几条内容”当成检索成功，也不会只看第一条结果分数高不高。检索阶段真正要判断的是：这些资料是否和当前需求匹配，是否来自可识别的来源，以及是否覆盖后续回答所需要的关键问题。

第一版我会先使用基于规则的质量检查，而不是马上增加一个专门的大语言模型评审。因为很多明显的问题不需要模型判断：没有结果、目的地不匹配、没有来源标识，或者必要子问题完全没有证据，都可以先用确定的规则发现。

具体来说，我会先看是否有结果，再看排名第一的重排序分数和前三条的平均分数，用来了解最相关结果和靠前候选的整体情况。同时检查元数据是否匹配用户的要求，例如用户查的是四姑娘山，结果不能只是其他地区的相似旅行内容。

来源标识也需要检查。我会把它作为质量信号，因为检索结果不能只剩下一段没有出处信息的文字。不过，有来源标识只说明这条资料有可追踪的身份，不等于资料中的所有内容都正确，更不等于后面生成的每一句话都已经被验证。检索质量检查和答案事实核验不能混为一谈。

复杂任务还要检查证据覆盖。比如用户同时要求交通比较、景点安排和预算验证，不能因为交通资料特别丰富，就认为整次检索已经足够。预算子任务没有可用证据时，这个需求仍然有缺口，即使总共已经返回了很多文档。

重排序分数阈值也不能直接照抄其他项目。不同重排序模型的分数分布不同，因此不能拿到一个零点七的阈值，就默认它在当前模型和数据上也有同样含义。这个分数首先是当前评分过程的输出，不应该未经验证就解释成答案正确的概率。

我会准备与实际业务接近的查询和文档样本，区分相关的正样本和不相关的负样本，再用准备使用的重排序模型对它们评分，观察两类分数的分布。阈值的选择要回到这些样本上看：哪些不相关内容会被放过去，哪些其实有用的内容会被拦下来。

阈值设得严格一些，可能减少不合适资料进入后续流程，但也可能拦掉部分可用结果；设得宽松一些，则可能保住更多结果，同时带入更多无关内容。因此，阈值不是一个脱离业务目标的固定答案，而是需要结合自己的评测样本和错误类型来确定。没有完成这一步验证之前，我不会把某个阈值说成已经适合当前业务。

对于没有通过检查的结果，我会把失败原因保留下来，比如“没有命中目的地”“否定条件没有满足”“预算子任务缺少资料”，再交给后续纠正性改写。这样第二次检索才知道应该修正什么，而不是对着同一个问题盲目再查一次。

所以，我会把这一层看作进入生成之前的证据检查：**有结果、相关性合适、条件匹配、来源可追踪、必要任务被覆盖，才有理由继续往后走。**即使检索检查通过，原方案后面仍然保留规划和校验环节，不能把“检索通过”夸大成“最终答案必然正确”。

**原文依据：**第 11.1—11.3 节、第 12 节、第 20 节。

---

## 10. 第一次检索失败后，系统应该怎样纠正查询并重新检索？

**面试问题：**纠正性改写与普通改写有什么区别，什么时候使用假设文档嵌入兜底，如何避免把假设文档当成真实证据，以及防止重试失控？

### 参考答案

我会先判断第一次检索为什么失败，再决定怎么改查询。不能一看到结果不好，就让模型随便把原问题再说一遍，因为第二次查询如果没有利用失败信息，很可能还是找回同一批不合适的资料。

普通的上下文改写，主要解决的是用户问题本身不完整，例如“那里”指哪里、“改成三天”修改的是哪份计划。纠正性改写面对的情况不同：系统已经执行过一次检索，也已经知道结果存在什么不足，所以它需要利用这次执行反馈调整下一次检索。

纠正性改写除了参考原始需求，还要看到第一次查询、第一次检索结果的标题或元数据，以及失败原因。比如用户问“成都周边适合看雪山又不徒步的地方”，第一次却找回很多高强度徒步路线。这时问题不只是文字表达不够丰富，而是“不徒步”这个否定条件没有得到满足。

第二次查询就可以把这个限制表达得更明确，例如“成都周边可乘车到达、无需徒步的雪山观景地点”。这里生成的仍然是一条检索请求，并不是声称某个地点已经具备这些条件。是否真的有符合要求的地点，仍然要通过检索到的资料判断。

次数上，我会先把纠正性重试限制为最多一次。也就是说，第一次检索失败后，可以根据反馈修正一次，再检索并检查结果，但不要让“改写、检索、再改写、再检索”无限循环下去。LangGraph 的状态中也会保留重试次数和检索状态，让恢复过程是明确可见的。

如果普通检索和纠正性检索仍然不足，我才会考虑假设文档嵌入。它适合用户描述比较模糊、关键词较弱的情况。比如用户想找“川西秋天那种蓝得很明显又比较容易到达的湖”，系统可以先生成一段符合这种描述的假设性文字，再用 BGE-M3 把这段文字转成稠密向量，去真实知识库里寻找相关资料。

这里最重要的边界是，**假设文档只是帮助检索的提示，不是检索证据，也不是已经核实的答案。**它是模型生成出来的，里面可能包含错误假设。如果直接把这段文字交给答案模型当成事实依据，就把原本用于找资料的辅助内容变成了未经验证的知识来源。

因此，我会把假设文档和真实检索结果分开保存。原报告的状态结构也分别保留了假设文档和证据字段。只有从知识库实际返回、带有来源信息、并且能够满足相关需求的资料，才有资格进入后续证据集合，不能因为一段假设文字写得像攻略，就把它当作真实攻略。

它之所以放在最后，是因为假设方向一旦错了，检索也可能被带偏。先用用户的真实问题检索，再根据明确失败原因纠正，最后才用假设文档补救，比一开始让所有问题都走假设文档生成更符合这份方案的风险控制思路。

所以我的处理逻辑是：**先定位失败原因，再有针对性地改写一次；仍然不足时，才考虑用假设文档帮助寻找真实资料。无论用了哪种恢复办法，都必须保留证据边界和明确的停止条件。**恢复机制的目的，是补齐资料，不是通过不断生成内容掩盖资料不足。

### 原文边界

关于终止条件，也要把原文讲清楚。它明确限制了一次纠正性重试，但没有完整定义假设文档兜底阶段的统一重试次数、总超时配置，以及兜底仍然失败时的最终分支。所以不能声称整套失败恢复机制已经被报告完整实现。能够确定的是，不能因为需要给出答案，就把假设内容冒充证据；如果真实依据仍然不足，就不能宣称已经完成了有证据支撑的回答。

**原文依据：**第 12.1 节及第 12 节正文、第 13.1—13.3 节、第 14 节、第 15 节、第 20 节。原文未完整定义假设文档兜底失败后的终止分支和总调用预算。

---

## 原文支持范围说明

以上回答只依据所提供的技术报告展开，没有引入外部论文、现行框架接口或未提供的项目测试数据。报告中的技术路线可以用于解释设计思路，但不能据此宣称已经完成全部功能，也不能补写召回率、准确率、延迟、成本降幅或线上效果。

原报告没有完整给出的内容，主要包括：改写与路由组合时的唯一执行顺序、完整的语义一致性校验、去重键与合并代码、有依赖关系的多跳任务调度、排名融合的具体计算参数、已经校准的质量阈值，以及假设文档兜底失败后的最终终止分支。面试涉及这些细节时，应区分“报告提出的方案”“需要继续实现的部分”和“已经有结果证明的能力”。
